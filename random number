#include<iostream>
#include<cstdlib>
#include<ctime>
using namespace std;
main() {
   
   int max;
   max = 3; //set the upper bound to generate the random number
   srand(time(0));
   for(int i = 0; i<=2; i++) { //generate 10 random numbers
      cout << "Dice" <<rand()% max + 1 << endl;
   }
}
